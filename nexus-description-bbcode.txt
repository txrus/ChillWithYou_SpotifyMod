[size=5][b]Spotify inside Chill with You: Lo-Fi Story[/b][/size]

Play, pause and skip, shuffle and repeat, control the volume, and drag the progress bar to seek. See what's playing with its cover art, search for songs, artists, albums and playlists, browse an artist's albums, open your own playlists or your Liked Songs, and switch playback to another device — all from the pause menu, without alt-tabbing out.

[line]

[size=4][b]New in v1.5[/b][/size]

[b]No building. This download is a ready-made DLL now.[/b]

Until now the mod needed your Spotify Client ID compiled into it, which meant installing the .NET SDK and building from source just to paste in one line. Several of you told me that was where you gave up, and fair enough.

The Client ID now goes in a text file instead:

[code]BepInEx\config\com.pw_txr.spotifyplayer.cfg[/code]

Drop in the DLLs, run the game once, paste your ID into that file, restart. No .NET SDK, no GameDir.props, no C#.

And if you forget the Client ID, the panel tells you so when you press Connect, instead of opening a browser page that could only fail.

[b]Earlier, in v1.4:[/b]

[list]
[*]Shuffle and repeat buttons next to the transport controls. Hover either one to see its name — the icons alone are too small to read.
[*]A volume slider sits next to the buttons too.
[*]A Devices button shows every device Spotify sees and lets you switch playback to one of them — no more "nothing happens" when Spotify has no active device.
[*]Hover the progress bar to preview the time you're about to seek to, before you click.
[*]Long playlists no longer lose track of what's playing once you're past the first 21 songs.
[*]A "Liked Songs" row in My Lists plays your saved tracks from the top, same as any playlist — and you can click a specific track in the queue to jump straight to it.
[/list]

[b]Earlier, in v1.3:[/b]

[list]
[*]Change the song on your phone, or in the Spotify app on the same PC, and the panel catches up within a few seconds. It only checks while the panel is open — close the menu and it stops entirely.
[*]Drag or click the progress bar to jump to a spot in the song.
[/list]

[line]

[size=4][b]Already running an older version?[/b][/size]

Your old DLL had the Client ID baked inside it. This one doesn't, so you do need to put your ID in the config file — it's the one extra step for upgraders, and after that you never touch it again.

[list=1]
[*]Copy both DLLs from this download over the old ones in [b]BepInEx\plugins[/b].
[*]Launch the game once, then close it.
[*]Open [b]BepInEx\config\com.pw_txr.spotifyplayer.cfg[/b] and paste the same Client ID you were building with.
[/list]

Your saved login survives the upgrade. You will not have to approve anything in the browser again.

[line]

[size=4][b]Before you start[/b][/size]

You need:

[list]
[*][b]Spotify Premium.[/b] Spotify only allows playback control (play / pause / skip) on Premium accounts. Free accounts can log in but cannot control anything.
[*][b]Windows.[/b] Your login is saved encrypted with Windows DPAPI.
[*][b]BepInEx 5.x (x64).[/b]
[*][b]A free Spotify developer app[/b] — yours, not mine. Spotify ties the permission prompt and the usage limits to whoever registered the app, so it can't be shipped with the mod. Making one takes two minutes and is step 2 below.
[/list]

That's the whole list. [b]No .NET SDK and no compiler since v1.5.[/b]

[line]

[size=4][b]1. Install BepInEx and the mod[/b][/size]

[list=1]
[*]Download BepInEx 5.x (x64) from [url=https://github.com/BepInEx/BepInEx/releases]the BepInEx releases page[/url].
[*]Extract it into your game folder, next to [b]Chill With You.exe[/b]. To find that folder in Steam: right-click the game > Manage > Browse local files.
[*]Launch the game once, then close it. BepInEx creates its folders on that first run, including [b]BepInEx\plugins[/b].
[*]Extract this download and copy [b]both[/b] files into [b]BepInEx\plugins[/b]:
[code]ChillWithYou_SpotifyMod.dll
System.Security.Cryptography.ProtectedData.dll[/code]
[/list]

The second one encrypts your saved login. Without it, Spotify login fails right after you approve in the browser.

[line]

[size=4][b]2. Create your Spotify app[/b][/size]

Free, about two minutes. The mod talks to Spotify through their official Web API, which requires an app registration. Yours is private.

[list=1]
[*]Go to the [url=https://developer.spotify.com/dashboard]Spotify developer dashboard[/url] and log in.
[*]Click [b]Create app[/b]. Name and description can be anything.
[*]In [b]Redirect URIs[/b], enter this exactly, including the trailing slash:
[code]http://127.0.0.1:8901/callback/[/code]
[*]Under [b]Which API/SDKs are you planning to use?[/b], tick [b]Web API[/b].
[*]Save, then open the app's Settings page and copy the [b]Client ID[/b] — a 32-character string of letters and numbers.
[/list]

You only need the Client ID. You do [b]not[/b] need the Client Secret: this mod uses PKCE, which is designed for apps that cannot keep a secret.

[line]

[size=4][b]3. Paste your Client ID into the config file[/b][/size]

[list=1]
[*]Launch the game once with the mod installed, then close it. That first run creates the config file:
[code]BepInEx\config\com.pw_txr.spotifyplayer.cfg[/code]
[*]Open it in any text editor (Notepad is fine). Find:
[code][Spotify]
ClientId =[/code]
[*]Paste your Client ID after the "=", with no quotes:
[code][Spotify]
ClientId = a1b2c3d4e5f6...[/code]
[*]Save and close the file.
[/list]

The mod reads this once when the game starts, so edit it with the game closed — or restart the game after editing.

[line]

[size=4][b]4. Play[/b][/size]

[list=1]
[*]Start Spotify on the same PC and play anything, so Spotify has an active device to control.
[*]Launch the game. The Spotify panel is in the pause / settings menu.
[*]Click [b]Connect Spotify[/b]. Your browser opens Spotify's permission page — approve it, then go back to the game.
[/list]

You only log in once. The session is remembered and resumes next time.

[line]

[size=4][b]If something goes wrong[/b][/size]

[b]The panel says "no Spotify Client ID set"[/b]

The config file is empty, or the ID didn't save. Check that you edited [b]BepInEx\config\com.pw_txr.spotifyplayer.cfg[/b] inside your [b]game[/b] folder, that the line reads [code]ClientId = yourid[/code] with no quotes, and that you restarted the game after saving.

[b]I pasted the ID but the mod still doesn't see it[/b]

The file is read only at startup, so the game must be restarted. If you edited it while the game was running, the game may also have written its own copy back over yours on exit — close the game first, then edit.

[b]There's no config file in BepInEx\config[/b]

The mod isn't loading at all. Check that both DLLs are in [b]BepInEx\plugins[/b], and that BepInEx itself is installed correctly — [b]BepInEx\LogOutput.log[/b] should exist after a run.

[b]Browser opens but login never completes[/b]

Two usual causes. First, the Redirect URI in your Spotify app must be exactly [code]http://127.0.0.1:8901/callback/[/code] — http (not https), and the trailing slash matters. Second, [b]System.Security.Cryptography.ProtectedData.dll[/b] must sit in [b]BepInEx\plugins[/b] next to [b]ChillWithYou_SpotifyMod.dll[/b].

You can confirm the second one: the player log will mention "Could not load file or assembly ... ProtectedData". It's at:
[code]%USERPROFILE%\AppData\LocalLow\nestopi\Chill With You\Player.log[/code]

[b]Buttons do nothing, playback won't change[/b]

Playback control needs Spotify Premium, and Spotify needs an active device. Make sure Spotify is open and playing on the same PC.

[b]Nothing appears in game[/b]

Check [b]BepInEx\LogOutput.log[/b] for lines tagged [code][Spotify Player Mod][/code] and feel free to post that log in the Bugs tab — it says which Client ID the mod picked up, partly hidden, so it's safe to paste.

[line]

[size=4][b]What Spotify won't let the mod do[/b][/size]

Spotify removed a number of Web API endpoints for apps like this one, so a few things are simply not possible:

[list]
[*][b]An artist's top tracks.[/b] Their album list does work — that's what expands when you click an artist — but "most popular songs" doesn't.
[*][b]Picking a specific track while playing from an artist.[/b] The queue is display-only there; use the next button.
[*][b]Reading the track list of most playlists[/b], including Daily Mix, Discover Weekly and "This Is ..." — so clicking a playlist plays it rather than opening it. Once it starts, the real queue shows up.
[*][b]Long result lists.[/b] Search returns at most 10 per category, and an artist's albums come back 10 at a time (the mod pages through up to 50 for you).
[*][b]Saving/liking a song, or browsing the full list of your Liked Songs.[/b] The endpoint for reading or writing that list returns an error in this app's mode, confirmed and not fixable in code. You can still play Liked Songs from My Lists in the panel — it just can't be browsed track-by-track before you do.
[/list]

Full details and links to Spotify's announcements are in the README.

[line]

[size=4][b]Source, issues, credits[/b][/size]

Source, issue tracker and version history: [url=https://github.com/txrus/ChillWithYou_SpotifyMod]github.com/txrus/ChillWithYou_SpotifyMod[/url]

You can still build it yourself if you'd rather — the repository has [b]build.ps1[/b], which builds in one command. Building no longer needs a Client ID; it goes in the same config file either way.

I made this mod to learn, so if something's broken feel free to fork it, or tell me and I'll try to fix it.

Not affiliated with the developers of Chill with You: Lo-Fi Story, or with Spotify. Licensed MIT.
